pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh "rm -rf depogithub"
                sh "git clone https://github.com/MUNGRAH-Kshemendra/depogithub.git"
            }
        }

    }
}
