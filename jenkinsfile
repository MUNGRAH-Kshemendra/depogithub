pipeline {
    agent any
    stages {

        stage('Clean') {
            steps {
                cleanWs()
            }
        }

        stage('Clone Repo') {
            steps {
                sh "git clone https://github.com/MUNGRAH-Kshemendra/depogithub.git"
            }
        }
    }
}
