pipeline {
    agent any


    stages {
     stage('Checkout-SCM') {
         steps {
             cleanWs()
             git branch: 'main', credentialsId: 'kshemendra-github-token', url: 'https://github.com/MUNGRAH-Kshemendra/depogithub.git'
             }
         }

         stage('Build-image-nginx') {
             steps {
             sh 'docker build -t mon-apache .'
             }
         }
     }
}
