pipline {
    agent any
        stages {
            stages {
                cleanWs()
            }
        }
        stages('Cloner le Github'){
            steps{
                sh 'sudo apt autoremove docker.io docker-compose -y'
                sh 'sudo apt update'
                sh 'sudo apt install apt-transport-https ca-certificates curl software-properties-common -y'
                sh 'sudo apt install docker.io -y'
                sh 'sudo apt update'
                sh 'sudo chmod 66 /var/run/docker.sock'
                sh 'sudo usermod -aG docker $USER'
                sh 'sudo systemclt status docker.service'
            }
        }
    }
}